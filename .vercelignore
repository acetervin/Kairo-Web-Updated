# Don't exclude dist folder - it's needed for serverless functions
# Vercel will automatically exclude devDependencies from serverless functions
node_modules
.env*
# Exclude vite-related files from serverless function (only needed for build)
vite.config.*
client/**

